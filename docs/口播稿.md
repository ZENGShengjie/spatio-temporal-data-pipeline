# 5 分钟演示视频 · 口播稿（小抄）

> 这是精简版口播稿，把"录制指南"中的所有讲话文字整理到一处。
> 录制时打印一份在桌面，读稿时自然即可。
> 语速建议：170-200 字/分钟，时长约 5 分钟。

---

## Part 1：PPT 演示（90 秒）

### 【第 1 张：标题页】

大家好，我是曾圣杰。今天演示的是我的毕业设计——多源时空数据融合的城市人流异常检测与预测系统。这个项目基于北京出租车 GPS 数据，实现了从数据采集、深度学习预测，到端到端异常检测的完整流水线。

### 【第 4 张：核心数字】

先看几个关键数字。STF 模型经过 Optuna 超参调优后，测试集 MAE 从 598 降到 298，降低了 50%。在 V3 注入测试集（4% 注入率，混合突增/突降/持续模式）上，单路统计法 F1 = 0.791，三路融合（统计 + STF真实推理 + VAE）F1 达到 0.9165，相对单路提升 +15.8% 绝对值。API 端到端响应在缓存命中时仅需 9.8 毫秒，比冷启动快 130 倍。整个系统仅用 222K 参数，是同类模型的十分之一。

### 【第 6 张：系统架构】

系统分为五层。最底层是多源数据，包括出租车 GPS、气象、POI 和路网。往上是三个时空联合模型——STF、AGFormer 和 STGCN。再往上是四范式异常检测，融合了统计阈值、预测误差、VAE 和 Transformer 自编码器。最上层是 FastAPI 和 Streamlit，为用户和下游系统提供接口。

### 【第 7 张：技术难点】

我们遇到了四个主要挑战。第一，初期异常注入比例只有 0.03%，后来改为按比例反向计算才达标。第二，STF 模型加载链路过复杂，我们重写了推理代码才修好。第三，融合权重搜索空间是指数级的，我们用分阶段搜索把它降到了可计算范围。第四，小数据集上大模型容易过拟合，所以选择了轻量化的 STF 架构。

### 【第 11 张：STF 模型架构】

STF 的核心创新是时空分解——把时间和空间的建模分开处理，通过一个全局环境 token 编码节假日和天气等全局上下文。这样用 222K 参数就超过了 2.26M 参数的 AGFormer。

---

## Part 2：系统演示（150 秒）

### 【实时热力图】

现在看系统的实际运行效果。这是 32×32 的北京出租车流量热力图，红色是异常区域。我们切到早上 8 点——异常率立刻跳到 8% 以上，触发二级预警，这正是 CBD 和地铁口周边的早高峰聚集。再切到凌晨 3 点，异常率回落至 1% 以下。模型每次检测耗时仅 200 毫秒。

### 【异常事件查询】

系统同时维护了一个完整的历史异常事件库。当前查询到 1443 个时空连续事件。如果我们打开"包含零散异常"开关，事件数从 1443 暴涨到 4424——多出的近 3000 个是当时未能形成时空聚集的单点高分事件。所有事件支持一键导出 CSV。

### 【单网格时序】

再看单网格的细节分析。我们看中心网格 512——能看到清晰的早晚双峰。红色三角标记的是异常时刻，与曲线突变完全吻合。换到机场附近的网格 200，曲线模式完全不同：深夜仍有流量，符合机场 24 小时特征。这说明模型真正学到了每个区域自己的"正常模式"。

### 【地理地图】

最后是地理视角。所有异常事件映射到真实经纬度，红色是紧急、橙色是重要、黄色是一般。可以直观看到热点聚集在 CBD、中关村和机场快线区域。点击任意红点可以看到事件详情。

---

## Part 3：收尾（30 秒）

### 【第 20 张：总结与未来方向】

总结一下：这套系统实现了端到端的城市异常检测，从多源数据到可视化全流程覆盖。核心创新是 STF 时空分解架构和四路融合检测：在 V3 注入测试集（4% 注入率）上，三路融合 F1 达到 0.9165，相对单路统计法（F1=0.791）提升 +15.8% 绝对值。未来计划引入多城市预训练、自适应网格划分，以及端到端联合优化。感谢观看，欢迎提问。

---

## 完整文本字数统计

| Part | 字数 | 时长（按 190 字/分钟） |
|------|------|---------------------|
| Part 1 | 396 字 | 125 秒 |
| Part 2 | 378 字 | 119 秒 |
| Part 3 | 92 字 | 29 秒 |
| **总计** | **866 字** | **约 4.5 分钟** |

如果需要 5 分钟（300 秒）严格卡时，可以：
- 适当放慢语速（每分钟 170 字）
- 在每张 PPT 停顿 2-3 秒
- 系统演示各操作之间留 1 秒空白