# Spatio-Temporal Urban Computing Pipeline — Universal Dependencies
# 用途：聚合 week1-7 的所有依赖，一次性安装
# 安装：pip install -r requirements.txt
# Python 版本：3.10+（推荐 3.12，已在 EC2 Ubuntu 22.04 验证）

# ── 核心科学计算 ─────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
pyarrow>=14.0.0        # 读 parquet（Week2+ 特征工程）

# ── 深度学习 ─────────────────────────────────────────────────────────────────
torch>=2.1.0
torch-geometric>=2.5.0 # GCN / GAT / STGCN（Week3-4）

# ── 时序模型 ─────────────────────────────────────────────────────────────────
statsmodels>=0.14.0    # ARIMA（Week3）
prophet>=1.1.0         # Prophet（Week3）

# ── 可视化 ───────────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.20.0

# ── Week6 API 服务化 ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
streamlit>=1.35.0
streamlit-autorefresh>=0.9.0
pydeck>=0.8.0
requests>=2.31.0

# ── Week7 Optuna + 可解释性 ──────────────────────────────────────────────────
optuna>=3.6.0
optuna-dashboard>=0.15.0
shap>=0.43.0           # SHAP 可解释性

# ── 工具 ─────────────────────────────────────────────────────────────────────
tqdm>=4.66.0            # 进度条
joblib>=1.3.0           # 并行
pyyaml>=6.0             # 配置文件
loguru>=0.7.0           # 日志（可选）

# ── 备注 ─────────────────────────────────────────────────────────────────────
# - Week1-2 数据下载脚本：requests + earthaccess + geopandas（按需单独安装）
# - geopandas:
#     conda install -c conda-forge geopandas  # 推荐 conda
# - rasterio (NDVI / 卫星影像):
#     conda install -c conda-forge rasterio
