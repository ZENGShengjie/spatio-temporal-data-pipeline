# Week6 任务2 依赖（Optuna 超参优化）
# 在 EC2 miniforge 环境内执行：
#   pip install -r week6.evaluation/optimization/requirements_optuna.txt

# 核心
optuna>=3.6.0          # 搜索框架
optuna-dashboard>=0.15.0  # 可选：可视化 dashboard

# 与 week4 训练保持一致的依赖
torch>=2.0.0           # 训练
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0        # 读 parquet

# 可视化（参数重要性图）
plotly>=5.18.0
matplotlib>=3.7.0
