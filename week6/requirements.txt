# Week6 工程化交付 — 依赖清单
# 生成时间: 2026-07-20
# 建议在 EC2 上新建 conda 环境后 pip install -r requirements.txt

# ── 核心依赖（已有项目依赖）─────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
torch>=2.0.0

# ── Week6 API & 可视化服务依赖 ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
streamlit>=1.35.0
requests>=2.31.0
streamlit-autorefresh>=0.9.0
plotly>=5.20.0
pydeck>=0.8.0
